# Screenshots

The images the README's Mockups section points at. Names are fixed, because
the README references them by path:

| File | Screen |
|---|---|
| `home.png` | Home: the day, its tasks and its habits |
| `month.png` | Month view |
| `create-event.png` | Creating an event |
| `create-habit.png` | Creating a habit |
| `settings.png` | Settings |
| `help.png` | Help and feedback |
| `about.png` | About (not in the README grid, kept for release notes) |

Taken on a 412×892 device, which is the size the design is laid out for.
Replace them from the same device when the interface changes, so the six keep
looking like one set.
